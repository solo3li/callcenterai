# Pinned: GeminiLiveLLMService lives at pipecat.services.google.gemini_live.llm
# and the LiveKit transport at pipecat.transports.livekit — both paths are
# 1.x-only; older 0.0.x releases do not have them.
pipecat-ai[livekit,google]==1.8.1
redis>=5.0.0
aiohttp>=3.11.12
